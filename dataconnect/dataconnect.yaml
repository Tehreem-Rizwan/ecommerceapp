specVersion: "v1beta"
serviceId: "ecommerceapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ecommerceapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
